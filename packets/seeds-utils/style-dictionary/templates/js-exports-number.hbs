'use strict';

module.exports = {
{{#each properties as |property|}}
{{#each property as |category|}}
	{{#each category as |item|}}
	{{ item.name }}: {{{ item.value }}}{{#if @../last}}{{#unless @last}},{{/unless}}{{else}},{{/if}}
	{{/each}}
{{/each}}
{{/each}}
};
